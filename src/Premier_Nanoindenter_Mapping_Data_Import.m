function [output1,output2] = Premier_Nanoindenter_Mapping_Data_Import(base_file_directory,xpm_pattern,rows,columns,bundle_spacing,row_overlap,column_overlap)
    output1 = 2*x;
    output2 = 3*x;
end


%need to convert serpentine to lateral
%then convert lateral to full grid
%then exclude overlap